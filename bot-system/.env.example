# Proxy (required for visit jobs; credentials only if your proxy needs auth)
PROXY_HOST=
PROXY_PORT=
PROXY_USERNAME=
PROXY_PASSWORD=

# Redis (BullMQ)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# API server port
PORT=3000

# Browser: set to false to run headed (for manual captcha solving)
HEADLESS=true
